import { createClient } from "@/utils/supabase/client"

//creates the connection with supabase
const supa = createClient(); 

export const create_dummy_data = () => {
    //create 5 users and 5 busines and 5 providers
    
}


//create 5 quotes
  //assign them user, business, 
  
//create 2 bookings
  //assign them user, business, and provider_