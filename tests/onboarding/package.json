{
  "name": "onboarding-tests",
  "version": "1.0.0",
  "description": "Integration tests for onboarding flow",
  "scripts": {
    "test": "node run-onboarding-tests.js",
    "test:validation": "npx jest validation.test.ts --config jest.config.js",
    "test:integration": "npx jest onboarding-flow.test.ts --config jest.config.js",
    "test:all": "npx jest --config jest.config.js --runInBand"
  },
  "dependencies": {
    "@jest/globals": "^29.0.0",
    "jest": "^29.0.0"
  }
}